---
import Layout from '../layouts/Layout.astro';
import { getPageContacto, getSiteConfig, renderRichText } from '../lib/storyblok';

const page = await getPageContacto();
const site = await getSiteConfig();

const email        = page.email    || '';
const linkedin     = page.linkedin || site.linkedin || '';
const contactIntro = renderRichText(page.intro) || '<p>¿Tienes un proyecto en mente? Cuéntame.</p>';
---

<Layout title="Sandra González — Contacto">
  <div class="contact-wrapper">
    <h1>Contacto</h1>
    <div class="contact-intro richtext" set:html={contactIntro} />

    {email && (
      <a href={`mailto:${email}`} class="contact-email">{email}</a>
    )}

    <div class="contact-socials">
      {linkedin && (
        <a href={linkedin} target="_blank" rel="noopener" class="social-link">LinkedIn</a>
      )}
    </div>
  </div>
</Layout>

<style>
  .contact-wrapper { max-width: 48rem; padding-top: 4rem; }
  h1 {
    font-family: 'Trocchi', Georgia, serif;
    font-size: clamp(3.5rem, 8vw, 7rem); font-weight: 400;
    line-height: 1; color: var(--fg); margin-bottom: 2rem;
  }
  .contact-intro { font-size: 1.1rem; color: var(--fg-muted); margin-bottom: 3rem; line-height: 1.7; }
  .contact-intro :global(p) { margin-bottom: 0.8em; }
  .contact-intro :global(strong) { font-weight: 600; color: var(--fg); }
  .contact-intro :global(a) { color: var(--accent, #8B7355); text-decoration: underline; text-underline-offset: 3px; }
  .contact-email {
    display: block; font-size: clamp(1.1rem, 2.5vw, 1.6rem);
    color: var(--fg); text-decoration: none;
    border-bottom: 1px solid var(--border); padding-bottom: 0.5rem;
    margin-bottom: 4rem; transition: border-color 0.2s; width: fit-content;
  }
  .contact-email:hover { border-color: var(--border-strong); }
  .contact-socials { display: flex; gap: 2.5rem; }
  .social-link {
    font-size: 0.8rem; letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--fg-secondary); text-decoration: none; transition: color 0.2s;
  }
  .social-link:hover { color: var(--fg); }
</style>
